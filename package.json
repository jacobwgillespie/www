{
  "private": true,
  "scripts": {
    "build": "zola build && workbox injectManifest workbox-config.js",
    "build-preview": "zola build --base-url $DEPLOY_PRIME_URL && workbox injectManifest workbox-config.js"
  },
  "devDependencies": {
    "prettier": "^2.0.4",
    "workbox-cli": "^5.1.2"
  },
  "prettier": {
    "bracketSpacing": false,
    "printWidth": 120,
    "semi": false,
    "singleQuote": true,
    "trailingComma": "all"
  }
}
